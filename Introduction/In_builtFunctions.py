print(abs(-1.4)) # to get positive version of every number
print(bool(10)) # will give a true
print(bool(0)) # its always false if its 0 or None
print(dir()) # used for all the functions i can use on specific object i can use it on
print(help(abs(-1.2))) # gives all the information for absolute value function
print(eval('print("HI")')) # eval function takes in a string and runs it as a python code
print(exec('print("HI")')) # used for mostly for multi line codes
print(str(1)) # type cast as string, float, integer using str, float, int

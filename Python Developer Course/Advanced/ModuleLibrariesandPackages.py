import os

os.listdir()  # list all the files in the folder

os.__file__  # gets the location of the os module

# pip is also a library
pip
install
glob2

# difference between module library and packages is module is one single file like os.py, library is inbuilt packages and packages are for external 3rd party
